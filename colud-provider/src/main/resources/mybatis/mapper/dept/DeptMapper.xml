<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zh.mapper.dept.DeptMapper">

    <select id="findList" resultType="com.zh.api.entity.dept.Dept">
        SELECT * FROM t_dept
    </select>
<!--     <insert id="insertInfo" parameterType="com.zh.entity.demo.Demo"  useGeneratedKeys="true" keyProperty="id">
    INSERT INTO  `info`(`name`,`age`,`addr`)VALUES (#{name},#{age},#{addr});
    </insert> -->
    
</mapper>